apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: myapp
  namespace: argocd
spec:
  project: default
  
  source:
    repoURL: https://github.com/yeswanth9171/learning.git
    path: Deployment
  destination:
    server: https://kubernetes.default.svc
    namespace: syncmy
    
  syncPolicy:
    syncOptions:
    - CreateNamespace=true
    
    automated:
      selfHeal: true
      prune: true
      
      
    
